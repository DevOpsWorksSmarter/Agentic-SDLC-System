apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: agentic-sdlc

resources:
  - namespace.yml
  - deployment.yml
  - service.yml
  - ingress.yml

commonLabels:
  app.kubernetes.io/name: agentic-sdlc
  app.kubernetes.io/managed-by: kustomize
